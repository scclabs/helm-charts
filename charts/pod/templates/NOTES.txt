1. Get the application URL by running these commands:
{{- if .Values.ingress.domain }}
  http{{ if .Values.ingress.tls }}s{{ end }}://{{ include "ingress.host" . }}/lab?token={{ .Values.jupyter.token | default (include "jupyter.random_token" . ) }}
{{- end }}