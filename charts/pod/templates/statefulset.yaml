apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "pod.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: pod
    release: "{{ .Release.Name }}"
    {{- include "pod.labels" . | nindent 4 }}
    {{- if .Values.labels }}
    {{ toYaml .Values.labels | indent 4 }}
    {{- end }}
  {{- with .Values.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "pod.selectorLabels" . | nindent 6 }}
  minReadySeconds: 10 # 默认值是 0
  template:
    metadata:
      annotations:
        k8s.scclabs.dev/jupyter-token: {{ .Values.jupyter.token | default (include "jupyter.random_token" .) }}
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.annotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        app: pod
        release: "{{ .Release.Name }}"
        {{- include "pod.labels" . | nindent 8 }}
	      {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "pod.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.name }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          env:
          - name: GRANT_SUDO
            value: "yes"
          - name: JUPYTER_TOKEN
            value: {{ .Values.jupyter.token | default (include "jupyter.random_token" .) }}
          livenessProbe:
            httpGet:
              path: /
              port: http
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /
              port: http
            failureThreshold: 30
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- with .Values.volumeMounts }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.volumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
