apiVersion: v1
kind: Service
metadata:
  name: {{ include "pod.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pod.selectorLabels" . | nindent 4 }}

---

{{- if .Values.service.sshPort }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "pod.fullname" . }}-ssh
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pod.labels" . | nindent 4 }}
  annotations:
    k8s.scclabs.dev/ssh-password: {{ .Values.jupyter.token | default (include "jupyter.random_token" .) }}
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector:
    {{- include "pod.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.sshPort }}
      targetPort: 22
{{- end }}
